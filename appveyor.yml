install:
  - mkdir utils
  - cd utils
  - appveyor DownloadFile http://camlorn.net/appveyor/jom.exe
  - cd ..
  - set QTDIR=C:\Qt\5.6\msvc2015
  - set PATH=%PATH%;%QTDIR%\bin;C:\MinGW\bin;./utils

environment:
  matrix:
    - QMAKE_FLAGS: "CONFIG+=debug"
      QMAKE_SPEC : "-r -spec win32-msvc2015"
      VSVER: 14.0 # Visual Studio 2015
    - QMAKE_FLAGS: ""
      QMAKE_SPEC : "-r -spec win32-msvc2015"
      VSVER: 14.0 # Visual Studio 2015

before_build:
  # Prepare the out-of-source build directory.
  - mkdir %APPVEYOR_BUILD_FOLDER%/../lib

build_script:
  - '"C:\Program Files (x86)\Microsoft Visual Studio %VSVER%\VC\vcvarsall.bat" x86'
  - qmake Tempest/Tempest.pro  %QMAKE_SPEC%  %QMAKE_FLAGS% "CONFIG+=msvc_static"
  - jom.exe
  - qmake Examples/examples.pro -r -spec win32-msvc2015 %QMAKE_FLAGS%
  - jom.exe
  - echo %APPVEYOR_BUILD_FOLDER%/../lib

artifacts:
  - path: lib\Tempest.dll
  - path: lib\Tempest.lib
  - path: lib\Tempest.a
